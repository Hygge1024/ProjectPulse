<resources>
    <style name="Theme.ProjectPulse" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.ProjectPulse.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/white</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <item name="postSplashScreenTheme">@style/Theme.ProjectPulse</item>
    </style>
</resources>
